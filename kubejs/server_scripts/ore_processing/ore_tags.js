ServerEvents.tags("item", (event) => {
    // ORE_STONE_TYPES.forEach(stone => {
    //     ORE_MINERAL_TYPES.forEach(ore => {
    //         event.add(`${stone[0]}_${ore[0]}_ore` ,`forge:ores/${ore}`)
    //     })
    // })
})